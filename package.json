{
  "name": "m-intel-wms-rf",
  "version": "3.0.0",
  "private": true,
  "description": "M-INTEL WMS RF Android/PWA client",
  "scripts": {
    "cap:init": "npx cap init \"M-INTEL WMS RF\" com.mintel.wmsrf --web-dir=www",
    "android:add": "npx cap add android",
    "android:sync": "npx cap sync android",
    "android:debug": "npm run android:sync && cd android && gradlew.bat assembleDebug",
    "android:release": "npm run android:sync && cd android && gradlew.bat assembleRelease"
  },
  "dependencies": {
    "@capacitor/android": "^7.4.0",
    "@capacitor/core": "^7.4.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^7.4.0"
  }
}
